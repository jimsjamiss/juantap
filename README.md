# juantap
# JuanTap Backend Setup

### Run Flask locally
1. cd backend
2. python -m venv .venv
3. .venv\Scripts\activate
4. pip install -r requirements.txt
5. python train_route_risk_model.py

Then your local API runs at:
http://127.0.0.1:5000/safe-route